<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Login</title>
    <style>
        body {
            background: linear-gradient(135deg, #e0f7fa 0%, #80deea 100%);
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .login-container {
            background-color: #ffffff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 350px;
            position: relative;
        }
            .login-container img {
                position: absolute;
                top: -40px; 
                left: 50%; 
                transform: translateX(-50%); 
                width: 80px; 
                z-index: 2;
            }
            .login-container:before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: rgba(0, 121, 107, 0.1);
                border-radius: 10px;
                z-index: 0;
            }
            .login-container h2 {
                color: #00796b;
                margin-bottom: 20px;
                position: relative;
                z-index: 1;
            }
        .form-group {
            margin-bottom: 15px;
            position: relative;
        }

        .form-label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #004d40;
            position: relative;
        }

        .form-input {
            width: 100%;
            padding: 10px;
            border: 1px solid #b2dfdb;
            border-radius: 5px;
            box-sizing: border-box;
            background-color: #f1f8e9;
            font-size: 14px;
            position: relative;
        }

        .btn-login {
            background-color: #00796b;
            color: #ffffff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
            transition: background-color 0.3s;
        }
            .btn-login:hover {
                background-color: #004d40;
            }
        .text-danger {
            color: #e53935;
            font-size: 12px;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <img src="~/images/journal-book.png" alt="Journal Image" />
        <h2 class="welcome-title">Увійдіть, щоб редагувати та додавати оцінки учнів</h2>
        <form asp-controller="Student_grade_managment" asp-action="Login_to_editor" method="post" class="login-form">
            <div class="form-group">
                <label for="TeacherUsername" class="form-label">Логін</label>
                <input id="TeacherUsername" name="TeacherUsername" class="form-input" type="text" placeholder="Введіть логін" />
                <span class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="TeacherPassword" class="form-label">Пароль</label>
                <input id="TeacherPassword" name="TeacherPassword" class="form-input" type="password" placeholder="Введіть пароль" />
                <span class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn-login">Увійти</button>
            </div>
        </form>
    </div>
</body>
</html>
